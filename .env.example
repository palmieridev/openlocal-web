# Copy to .env.local and fill in. Do not commit real secrets.

# Clerk — get keys from the Clerk dashboard for the "Openlocal API" app.
PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx

# Clerk auto-proxy opt-out. @clerk/backend points the handshake at a same-origin
# /__clerk proxy on Vercel production builds, but that route only exists in the
# Next.js SDK — Astro 404s and signed-in users get locked out. Set in Vercel
# (Production + Preview); harmless locally, where a pk_test key never triggers it.
CLERK_DISABLE_AUTO_PROXY=true

# Clerk hosted-flow routing
PUBLIC_CLERK_SIGN_IN_URL=/login
PUBLIC_CLERK_SIGN_UP_URL=/register
PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/dashboard
PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# Openlocal API base URL.
# Local:  http://localhost:8080
# Tunnel: https://grandma-renewed-clunky.ngrok-free.dev
PUBLIC_API_BASE_URL=http://localhost:8080

# Vercel Blob — image uploads (business cover). Optional: the upload route
# returns 503 when unset. Get it from the Blob store's ".env.local" tab in the
# Vercel dashboard, or `vercel env pull`. Read at runtime, so the k8s dev image
# takes it from a Secret rather than a build arg.
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxx

# Sentry error monitoring (optional — integration is skipped without a DSN).
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# Source-map upload (CI only; needs org+project too):
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=
